# 日历应用 - Calendar App

一个基于 Vue 3 开发的现代化日历应用，支持多视图展示和完整的事件管理功能。

## 🚀 功能特性

### ✅ 已实现功能

#### 核心功能

- **多视图日历展示**

  - 月视图：完整的月份网格布局，支持事件预览
  - 周视图：时间轴式的事件排布
  - 日视图：详细的时间槽和事件显示

- **完整的事件管理**

  - 事件添加：支持标题、描述、时间、颜色、提醒设置
  - 事件编辑：在原始数据基础上进行修改
  - 事件查看：详细的事件信息展示
  - 事件删除：安全的删除确认机制

- **智能事件显示**
  - 月视图：事件以彩色圆点显示，支持"更多"展开
  - 周/日视图：时间轴上的精确事件定位
  - 跨日期事件支持

#### 用户体验

- **响应式设计**：适配桌面和移动设备
- **直观操作**：点击日期/时间槽快速创建事件
- **视觉反馈**：操作成功/失败提示
- **颜色分类**：6 种预定义颜色标识不同事件类型

### 🎯 待实现功能

- 日程提醒功能（浏览器通知）
- 数据导入导出（iCalendar 格式）
- 农历显示
- 网络订阅功能
- 数据持久化（本地存储）

## 🛠 技术栈

### 前端框架

- **Vue 3** - 渐进式 JavaScript 框架
- **Composition API** - 现代化的组件开发方式
- **Pinia** - 状态管理

### 开发工具

- **Vite** - 快速构建工具
- **date-fns** - 日期处理库

### 样式设计

- 纯 CSS 实现，无 UI 框架依赖
- CSS Grid & Flexbox 布局
- 响应式设计原则

## 📦 项目结构

```
src/
├── components/
│   ├── CalendarView/
│   │   ├── MonthView.vue      # 月视图组件
│   │   ├── WeekView.vue       # 周视图组件
│   │   └── DayView.vue        # 日视图组件
│   ├── EventForm.vue          # 事件表单（添加/编辑）
│   ├── EventDetail.vue        # 事件详情查看
│   └── DayEventsModal.vue     # 日期事件列表模态框
├── stores/
│   └── calendar.js           # 日历状态管理
├── views/
│   └── HomeView.vue          # 主页面
└── utils/                    # 工具函数（待扩展）
```

## 🎨 核心组件说明

### 状态管理 (Pinia Store)

```javascript
// 核心数据结构
{
  currentDate: Date,          // 当前显示日期
  events: Array,              // 事件列表
  viewMode: 'month'|'week'|'day', // 视图模式
  selectedEvent: Object       // 选中的事件
}
```

### 事件数据结构

```javascript
{
  id: String,                 // 唯一标识符
  title: String,              // 事件标题
  description: String,        // 事件描述
  startTime: Date,           // 开始时间
  endTime: Date,             // 结束时间
  color: String,             // 事件颜色
  reminder: String,          // 提醒设置
  createdAt: Date,           // 创建时间
  updatedAt: Date            // 更新时间
}
```

## 🚀 快速开始

### 环境要求

- Node.js 16+
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

## 📖 使用指南

### 添加事件

1. 点击右下角"+"按钮打开事件表单
2. 填写事件信息（标题、时间、颜色等）
3. 点击"创建事件"保存

### 查看事件

- **月视图**：点击事件彩色圆点
- **周/日视图**：点击事件块
- **事件过多**：点击"更多"查看完整列表

### 编辑事件

1. 在事件详情中点击"编辑"按钮
2. 在预填充的表单中修改信息
3. 点击"更新事件"保存更改

### 删除事件

1. 在事件详情中点击"删除"按钮
2. 确认删除操作
3. 事件将从日历中移除

### 视图切换

- 使用顶部标签在月、周、日视图间切换
- 点击日期可快速切换到日视图

## 🔧 开发说明

### 事件过滤逻辑

```javascript
// 基于当前视图范围过滤事件
const currentViewEvents = computed(() => {
  return events.value.filter((event) => {
    const eventDate = new Date(event.startTime);
    const range = currentViewRange.value;
    return eventDate >= range.start && eventDate <= range.end;
  });
});
```

### 日期处理

使用 `date-fns` 库进行复杂的日期计算：

- 月份范围计算
- 周开始/结束日期
- 日期比较和格式化

### 响应式设计

- 移动端优先的 CSS 设计
- 断点适配：桌面(>1024px)、平板(768px-1024px)、手机(<768px)

## 🎯 技术亮点

### 1. 组件化架构

- 高度可复用的日历视图组件
- 单一职责原则的组件设计
- 清晰的组件通信机制

### 2. 状态管理

- 集中式的日历状态管理
- 响应式数据流
- 类型安全的状态更新

### 3. 用户体验优化

- 流畅的视图切换动画
- 直观的操作反馈
- 错误边界处理

### 4. 代码质量

- 清晰的代码结构
- 详细的注释说明
- 可维护的组件设计

## 🔮 后续开发计划

### 短期目标

1. **提醒功能** - 集成浏览器通知 API
2. **数据持久化** - 本地存储集成
3. **iCalendar 支持** - 导入导出标准格式

### 长期目标

1. **多设备同步** - 云端数据同步
2. **协作功能** - 共享日历和事件
3. **插件系统** - 可扩展的功能模块

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request 来改进这个项目！

### 开发流程

1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 🙏 致谢

- [Vue.js](https://vuejs.org/) - 渐进式 JavaScript 框架
- [date-fns](https://date-fns.org/) - 现代 JavaScript 日期工具库
- [Pinia](https://pinia.vuejs.org/) - Vue 状态管理库

---

**开发状态**: 🟢 核心功能已完成  
**最后更新**: 2024 年 1 月  
**版本**: v1.0.0
